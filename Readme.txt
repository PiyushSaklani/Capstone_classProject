Put your functions in the respective folders.

Commands.

# To add git inside the folders
git init
git add .
git remote add origin https://github.com/{UserName}/{RepositoryName}.git
git branch -M main
git commit -m "{Any Msg}"
git push -u origin main